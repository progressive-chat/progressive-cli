#include "logger.hpp"

namespace matrixcli { namespace util {

// Logger is fully defined in the header (singleton with mutex).
// This file exists for future non-inline logger extensions.

}} // namespace matrixcli::util
